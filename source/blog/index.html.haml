---
blog: Blog
---
%header.mod-page-title
  %h1 Blog
  %h2 Technical write-ups
%article.mod-blog-overview
  - page_articles.each do |article|
    = link_to article.url do
      %h1= article.title
      - if article.data.subtitle
        %h2= article.data.subtitle
      %date= article.date.strftime('%b %e %Y')
